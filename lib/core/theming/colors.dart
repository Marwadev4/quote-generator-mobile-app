import 'package:flutter/material.dart';

class LightColors {
  // Primary
  static const Color primary = Color(0xFF5D13E7); // vividViolet
  static const Color primaryVariant = Color(0xFF8249B5); // moderateViolet
  static const Color primarySoft = Color(0xFFCFBBF0); // verySoftViolet

  // Backgrounds
  static const Color background = Color(0xFFFBFBFB); // veryLightGray
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF323232); // veryDarkGray
  static const Color textSecondary = Color(0xFF6E6E6E); // darkGray
  static const Color textDisabled = Color(0xFF9E9E9E);

  // Icons & Dividers
  static const Color icon = Color(0xFF6E6E6E);
  static const Color divider = Color(0xFFE0E0E0);

  // Overlay
  static const Color overlay = Color(0xB2FBFBFB); // veryLightGrayWithOpacity

  // Shimmer
  static const Color shimmerBase = Color(0xFF6351FD);
  static const Color shimmerHighlight = Color(0xFFB8B2FF);
}

class DarkColors {
  // Primary
  static const Color primary = Color(0xFF9B7BFF); // lighter vivid violet
  static const Color primaryVariant = Color(0xFF8249B5); // moderateViolet
  static const Color primarySoft = Color(0xFF3A2A5E); // darkened soft violet

  // Backgrounds
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color card = Color(0xFF252437);

  // Text
  static const Color textPrimary = Color(0xFF121212);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textDisabled = Color(0xFF6E6E6E);

  // Icons & Dividers
  static const Color icon = Color(0xFFB0B0B0);
  static const Color divider = Color(0xFF2C2C2C);

  // Overlay
  static const Color overlay = Color(0x80252437);

  // Shimmer
  static const Color shimmerBase = Color(0xFF252437);
  static const Color shimmerHighlight = Color(0xFF6351FD);
}
